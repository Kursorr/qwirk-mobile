export class TrackInfo {

  /**
   * @param {number} id
   * @param {string} name
   * @param {string} picture
   * @param {string} backgroundColor
   */
  constructor(id, name, picture, backgroundColor) {
    this.id = id
    this.name = name
    this.picture = picture
    this.backgroundColor = backgroundColor
  }
}
